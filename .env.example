# Sanity — i valori pubblici del progetto (gli stessi in sanity.config.ts)
NEXT_PUBLIC_SANITY_PROJECT_ID=qtdomht6
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-07-11

# URL base del sito. In locale lascia localhost: viene usato per canonical,
# sitemap, robots, feed RSS e JSON-LD.
NEXT_PUBLIC_BASE_URL=http://localhost:3000
